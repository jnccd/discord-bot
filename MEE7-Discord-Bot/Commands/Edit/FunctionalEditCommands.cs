using Discord.WebSocket;
using System;
using System.Collections.Generic;
using System.Drawing;
using System.Linq;
using System.Text;
using static MEE7.Commands.Edit;

namespace MEE7.Commands
{
    class FunctionalEditCommands : EditCommandProvider
    {
        //public string AddeDesc = "It adds E";
        //public string Adde(string s, SocketMessage m, int count = 1)
        //{
        //    return s + new string(Enumerable.Repeat('a', count).ToArray());
        //}

        //public string forLoopDesc = "Loopi Loopi";
        //public object[] ForLoop(object o, SocketMessage m, string pipe, string varName, float startValue, float endValue, float stepWidth)
        //{
        //    List<object> results = new List<object>();

        //    for (float f = startValue; f < endValue; f += stepWidth)
        //        results.Add(Pipe.Parse(m, pipe.Replace("%" + varName, f.ToString())).Apply(m, o));

        //    return results.ToArray();
        //}
    }
}
