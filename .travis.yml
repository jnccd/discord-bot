language: csharp
solution: TestDiscordBot.sln
jobs:
  include:
    - stage: build
      name: "Windows build"
      mono: none
      os: windows
    - name: "Linux build"
      os: linux
    - name: "Mac n Chesse build"
      os: osx
