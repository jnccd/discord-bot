language: csharp
solution: TestDiscordBot.sln
jobs:
  include:
    - stage: build
      name: "Windows build"
      mono: none
      dotnet: 2.1.502
      script:
       - dotnet restore
      os: windows
    - name: "Linux build"
      os: linux
    - name: "Mac n Chesse build"
      os: osx
