stages:
    - deploy

deploy:
    image:
        name: docker/compose:1.24.1
        entrypoint: ["/bin/sh", "-c"]
    stage: deploy
    script: 
        - docker-compose build
        - BotToken=$BotToken CI_SERVER=$CI_SERVER docker-compose up
