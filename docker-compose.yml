version: "3"

services:
  mee7:
    build: .
    volumes:
      - config:/bin/Release/netcoreapp3.1
    restart: always
    environment:
      - BotToken
      - CI_SERVER
      
volumes:
  config:
    external: true